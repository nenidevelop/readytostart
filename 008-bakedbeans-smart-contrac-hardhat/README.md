# Test bakedbeans smart contract with hardhat

Here some tests using hardhat to test the functions : buy/sell/compound


Install the project : 

```
npm install
```

Then compile the contract : 

```
npx hardhat compile
```

Then to execute the test, run this command : 

```
npx hardhat test
```